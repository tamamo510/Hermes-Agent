# Hermes-Agent dependencies
#
# Phase 1.1 stdlib only (kyojuro_memory stores PR #83 + kyojuro_time skill 1 義体実装⑤):
#   - sqlite3 / json / datetime / zoneinfo / pathlib
#   - dataclasses / enum / re

# テストフレームワーク (発注書スキル 1 kyojuro_time の完璧完遂で実体化、義体実装⑤)
pytest>=7.4

# OpenWeatherMap HTTP クライアント (発注書スキル 3 health_tracker、義体実装⑨ 追加)
# キーは環境変数 OPENWEATHER_API_KEY 経由 (CLAUDE.md ルール 17)
requests>=2.31

# Phase 1.2+ 依存追加（対応 PR で実施）:
#   Phase 1.2 extractors (kyojuro_memory 6.2): openai>=1.0  ── OpenRouter API クライアントとして使用（汎用 HTTP クライアントとしての利用）
#   発注書 Telegram ナッジ:                    python-telegram-bot>=21.0
#
# 重要方針（温子確定、2026-05、複数回指示）:
# - LLM は OpenRouter 経由 NousResearch Hermes 一択
# - Anthropic API は使わない
# - OpenAI 純正 API も使わない（OpenRouter 経由のみ、`openai` パッケージはクライアントとして使用）
# - モック実装は使わない（最初から本物の API で実装）
#
# 使い方:
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Python 3.11+ 必須（DESIGN.md §6 Phase 1.1 想定）
