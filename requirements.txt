# kyojuro_memory dependencies
#
# Phase 1.1 (PR #83): stores layer uses Python stdlib only
#   - sqlite3 (stdlib)
#   - json (stdlib)
#   - datetime (stdlib)
#   - pathlib (stdlib)
#
# Phase 1.2+ 依存追加（対応 PR で実施）:
#   Phase 1.2 extractors: openai>=1.0  ── OpenRouter API クライアントとして使用（汎用 HTTP クライアントとしての利用）
#   Phase 1.4 tests:      pytest>=7.4
#   発注書スキル 5 calendar_manager: requests>=2.31  ── OpenWeatherMap API
#   発注書 Telegram ナッジ: python-telegram-bot>=21.0
#
# 重要方針（温子確定、2026-05、複数回指示）:
# - LLM は OpenRouter 経由 NousResearch Hermes 一択
# - Anthropic API は使わない
# - OpenAI 純正 API も使わない（OpenRouter 経由のみ、`openai` パッケージはクライアントとして使用）
# - モック実装は使わない（最初から本物の API で実装）
#
# 使い方:
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Python 3.11+ 必須（DESIGN.md §6 Phase 1.1 想定）
